services:
  shortly:
    container_name: shortly
    build:
      context: ./src
      dockerfile: Dockerfile
    restart: always
    env_file:
      - ./src/.env
    volumes:
      - ./src/logs:/usr/src/logs
      - static_volume:/usr/share/nginx/html
      - media_volume:/usr/src/media
    networks:
      - shortly_net
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  shortly_asgi:
    container_name: shortly-asgi
    build:
      context: ./src
      dockerfile: Dockerfile
    command: daphne -b 0.0.0.0 -p 8001 core.asgi:application
    env_file:
      - ./src/.env
    volumes:
      - ./src/logs:/usr/src/logs
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - shortly_net
    restart: always

  nginx:
    container_name: shortly-nginx
    build: 
      context: ./infra/nginx
      dockerfile: Dockerfile
    restart: always
    ports:
      - "${NGINX_PORT:-8080}:80"
    volumes:
      - ./infra/nginx/shortly.prod.conf:/etc/nginx/conf.d/default.conf
      - ./infra/nginx/whitelist.conf:/etc/nginx/whitelist.conf:ro
      - ./infra/nginx/maintenance.html:/usr/share/nginx/maintenance/maintenance.html:ro
      - ./infra/nginx/maintenance.flag:/usr/share/nginx/maintenance/maintenance.flag
      - static_volume:/usr/share/nginx/html
      - media_volume:/usr/share/nginx/media
    depends_on:
      shortly:
        condition: service_healthy
    networks:
      - shortly_net

  celery_worker:
    build: 
      context: ./src
      dockerfile: Dockerfile
    container_name: shortly-celery-worker
    env_file:
      - ./src/.env
    command: celery -A core.celery worker -l info 
    working_dir: /usr/src
    volumes:
      - ./src:/usr/src
    environment:
      - PYTHONPATH=/usr/src
    depends_on:
      shortly:
        condition: service_healthy
    networks:
      - shortly_net
    restart: always

  celery_beat:
    build: 
      context: ./src
      dockerfile: Dockerfile
    container_name: shortly-celery-beat
    command: celery -A core.celery beat -l info
    working_dir: /usr/src
    volumes:
      - ./src:/usr/src
    env_file:
      - ./src/.env
    depends_on:
      shortly:
        condition: service_healthy
    networks:
      - shortly_net
    restart: always

  flower:
    build:
      context: ./src
      dockerfile: Dockerfile
    container_name: shortly-flower
    env_file:
      - ./src/.env
    command: >
      celery --app=core.celery flower --port=5555
      --basic_auth=${FLOWER_USER}:${FLOWER_PASSWORD}
    ports:
      - "5556:5555"
    working_dir: /usr/src
    depends_on:
      celery_worker:
        condition: service_started
      celery_beat:
        condition: service_started
    networks:
      - shortly_net
    restart: always

  redis:
    image: redis:7.0-alpine
    container_name: shortly-redis
    env_file:
      - ./src/.env
    restart: always
    command: >
      redis-server --appendonly yes
                  --maxmemory 256mb
                  --maxmemory-policy allkeys-lru
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data
    networks:
      - shortly_net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
      
networks:
  shortly_net:
    driver: bridge

volumes:
  static_volume:
  media_volume:
  redis_data: